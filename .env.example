# Public site — copy to .env for local dev
# API base (use /api/v1 with Vite proxy, or full URL in production)
VITE_API_URL=/api/v1

# Vite dev proxy target (optional)
VITE_API_PROXY=http://localhost:8000
